version: "3.9"

services:

  poisson-app:

    build: .

    container_name: poisson-container

    ports:
      - "5000:5000"

    volumes:
      - .:/app

    environment:
      ENVIRONMENT: Docker

    restart: unless-stopped